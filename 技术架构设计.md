# 锅炉燃煤优化系统 - 技术架构设计

## 一、系统总体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    人机界面层 (HMI)                          │
│  - 实时监控、参数设定、报警显示、历史查询                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                  监控与决策层                                │
│  - 专家系统（规则引擎）                                      │
│  - 决策支持系统                                              │
│  - 可解释性分析（SHAP/LIME）                                 │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    优化控制层                                │
│  ┌──────────────┐         ┌──────────────┐                 │
│  │   APC控制    │         │  神经网络预测 │                 │
│  │  (MPC算法)   │ ←→      │  (LSTM模型)   │                 │
│  └──────────────┘         └──────────────┘                 │
│         ↓                        ↓                          │
│  ┌──────────────────────────────────────┐                   │
│  │      参数优化算法 (PSO/GA)           │                   │
│  └──────────────────────────────────────┘                   │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    协调控制层 (新增)                         │
│  - 控制权分配（优先级管理）                                  │
│  - 控制量融合（加权融合/最优选择）                           │
│  - 冲突检测与解决                                            │
│  - 故障检测与降级切换                                        │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    执行控制层                                │
│  ┌──────────────┐         ┌──────────────┐                 │
│  │   PID控制    │         │   安全保护    │                 │
│  │  (基础控制)  │ ←→      │  (硬/软限位)  │                 │
│  └──────────────┘         └──────────────┘                 │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    数据采集层                                │
│  - SCADA/DCS接口                                            │
│  - 数据预处理（滤波、归一化）                                │
│  - 数据质量检测                                              │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    数据存储层                                │
│  - 时序数据库（InfluxDB）- 实时数据                          │
│  - 关系数据库（PostgreSQL）- 配置和历史数据                  │
│  - 对象存储（MinIO）- 模型文件                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、控制流程设计

### 2.1 正常控制流程

```
1. 数据采集（100ms周期）
   ↓
2. 数据预处理和质量检测
   ↓
3. PID基础控制计算
   ↓
4. 神经网络状态预测（异步，10s周期）
   ↓
5. APC优化控制计算（1s周期）
   ↓
6. 参数优化算法（PSO/GA，5s周期）
   ↓
7. 协调层：控制量融合
   ↓
8. 安全保护检查（硬限位、软限位）
   ↓
9. 控制输出到执行器
   ↓
10. 效果评估和数据记录
```

### 2.2 故障降级流程

```
故障检测
   ↓
判断故障类型
   ├─→ APC故障 → 降级到Level 2（PID + 神经网络）
   ├─→ 神经网络故障 → 降级到Level 3（PID + APC）
   ├─→ 数据异常 → 降级到Level 3（使用历史平均值）
   └─→ 严重故障 → 降级到Level 4（仅PID）
   ↓
切换控制模式
   ↓
报警通知
   ↓
记录故障日志
```

---

## 三、模块详细设计

### 3.1 神经网络预测模块

**输入变量**：
- 当前负荷
- 煤质参数（热值、水分、灰分等）
- 当前控制参数（给煤量、风量、氧量等）
- 历史状态（温度、压力、NOx等）

**输出变量**：
- 预测温度（主蒸汽、再热蒸汽等）
- 预测压力
- 预测NOx排放
- 预测效率

**模型结构**：
```
Input Layer (特征标准化)
   ↓
LSTM Layer 1 (128 units, return_sequences=True)
   ↓
Dropout (0.2)
   ↓
LSTM Layer 2 (64 units)
   ↓
Dropout (0.2)
   ↓
Dense Layer (32 units, ReLU)
   ↓
Output Layer (多输出：温度、压力、NOx、效率)
```

**优化措施**：
- 模型量化（FP32 → FP16）
- 模型剪枝（移除<1%贡献的神经元）
- ONNX Runtime加速推理

### 3.2 APC控制模块

**控制算法**：模型预测控制（MPC）

**预测模型**：使用神经网络预测结果

**优化目标**：
```
min J = Σ [w1*(y-y_ref)² + w2*Δu² + w3*NOx²]
约束：
- 控制量范围：u_min ≤ u ≤ u_max
- 控制量变化率：|Δu| ≤ Δu_max
- 输出量范围：y_min ≤ y ≤ y_max
```

**控制周期**：1秒

### 3.3 参数优化模块

**优化算法**：粒子群算法（PSO）

**优化变量**：
- PID参数（Kp, Ki, Kd）
- 给煤量设定值
- 风量设定值
- 氧量设定值

**目标函数**：
```
min F = w1*能耗 + w2*NOx + w3*控制偏差 - w4*效率
约束：
- 安全限位
- 设备能力
- 环保指标
```

**优化周期**：5秒（在线优化）或离线优化

### 3.4 协调控制模块

**功能**：
1. **控制权分配**
   ```
   优先级（从高到低）：
   1. 安全保护（硬限位）
   2. PID基础控制
   3. 安全保护（软限位）
   4. APC优化控制
   5. 神经网络预测建议
   ```

2. **控制量融合策略**
   ```python
   # 加权融合
   u_final = α*u_PID + β*u_APC + γ*u_NN
   其中：α + β + γ = 1
        α > β > γ（PID权重最高）
   
   # 或选择策略
   if 安全保护触发:
       u_final = u_safe
   elif 控制偏差 > 阈值:
       u_final = u_PID
   else:
       u_final = u_APC
   ```

3. **冲突检测**
   - 检测各控制层输出是否冲突
   - 冲突时按优先级选择

### 3.5 安全保护模块

**硬限位保护**：
- 硬件限位开关
- 超限立即切断控制输出
- 响应时间：<10ms

**软限位保护**：
- 控制参数范围检查
- 变化率限制（防止突变）
- 多参数关联检查
- 响应时间：<100ms

**预测结果验证**：
- 预测值合理性检查（与历史数据对比）
- 异常预测值拒绝
- 使用历史平均值替代

---

## 四、数据流设计

### 4.1 实时数据流

```
SCADA/DCS
   ↓ (100ms)
数据采集模块
   ↓
数据预处理（滤波、归一化）
   ↓
数据质量检测
   ├─→ 合格 → 存储到InfluxDB
   └─→ 异常 → 报警 + 使用历史值
   ↓
实时数据缓存（Redis）
   ↓
各控制模块读取
```

### 4.2 历史数据流

```
InfluxDB（实时数据）
   ↓ (每小时)
数据归档（压缩存储）
   ↓
特征工程（自动提取）
   ↓
模型训练数据准备
   ↓
模型训练/更新
   ↓
模型验证
   ↓
模型部署（ONNX格式）
```

---

## 五、性能指标

### 5.1 实时性指标

| 模块 | 目标响应时间 | 实际要求 |
|------|------------|---------|
| PID控制 | <50ms | 100ms周期 |
| APC控制 | <200ms | 1s周期 |
| 神经网络预测 | <100ms | 10s周期（异步） |
| 参数优化 | <500ms | 5s周期 |
| 安全保护 | <10ms | 立即响应 |

### 5.2 精度指标

| 指标 | 目标值 |
|------|--------|
| 温度预测误差 | <2°C |
| 压力预测误差 | <0.1MPa |
| NOx预测误差 | <5mg/m³ |
| 控制精度 | 偏差<1% |

### 5.3 可靠性指标

| 指标 | 目标值 |
|------|--------|
| 系统可用性 | >99.9% |
| 故障切换时间 | <1s |
| 数据丢失率 | <0.01% |

---

## 六、技术栈选型

### 6.1 开发语言
- **Python 3.9+**：上层控制逻辑、机器学习
- **C++17**：实时控制、性能关键模块
- **SQL**：数据查询和管理

### 6.2 核心框架
- **PyTorch**：神经网络训练
- **ONNX Runtime**：模型推理加速
- **NumPy/SciPy**：数值计算
- **scikit-learn**：传统机器学习

### 6.3 数据库
- **InfluxDB 2.0**：时序数据存储
- **PostgreSQL**：配置和元数据
- **Redis**：实时数据缓存

### 6.4 通信协议
- **Modbus TCP/IP**：与DCS/SCADA通信
- **OPC UA**：工业标准通信
- **MQTT**：内部模块通信

### 6.5 部署环境
- **Docker**：容器化部署
- **Kubernetes**：容器编排（可选）
- **Linux**：运行环境

---

## 七、部署架构

```
┌─────────────────────────────────────┐
│        负载均衡器 (Nginx)            │
└─────────────────────────────────────┘
         ↓              ↓
┌──────────────┐  ┌──────────────┐
│   Web服务    │  │   API服务     │
│  (Flask)     │  │  (FastAPI)   │
└──────────────┘  └──────────────┘
         ↓              ↓
┌─────────────────────────────────────┐
│         控制服务集群                 │
│  - PID控制服务                       │
│  - APC控制服务                       │
│  - 神经网络预测服务                  │
│  - 参数优化服务                      │
│  - 协调控制服务                      │
└─────────────────────────────────────┘
         ↓
┌─────────────────────────────────────┐
│         数据服务层                   │
│  - InfluxDB（时序数据）              │
│  - PostgreSQL（配置数据）             │
│  - Redis（缓存）                     │
└─────────────────────────────────────┘
         ↓
┌─────────────────────────────────────┐
│         DCS/SCADA系统                │
└─────────────────────────────────────┘
```

---

## 八、关键设计原则

1. **安全性优先**：多层安全保护，故障自动降级
2. **实时性保障**：异步处理，关键路径优化
3. **可扩展性**：模块化设计，易于扩展
4. **可维护性**：清晰的架构，完善的日志
5. **可解释性**：提供决策依据，增强信任度

---

**设计完成日期**：2024年
